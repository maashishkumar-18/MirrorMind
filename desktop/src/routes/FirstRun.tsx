/** Placeholder — the first-launch model-selection flow lands in fe.6. */
export function FirstRun() {
  return (
    <main className="screen">
      <h1>Choose a model</h1>
      <p>The first-launch model flow is built in Phase 3 Step 3.1-fe.6.</p>
    </main>
  );
}
